{
  "name": "db-app",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest"
  },
  "dependencies": {
    "@prisma/client": "^7.0.1",
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "prisma": "^7.0.1",
    "vitest": "^1.0.0",
    "wrangler": "^4.4.0"
  }
}
